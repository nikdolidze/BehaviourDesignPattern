using System;
/*
Sender - Invokes the handler(invokes something in the application,so we have a handler that runs through a chain of receiver and receiver itself can be somthing that handles that given  command
Handler - Runs through the chain of receivers
Receiver - Handles the given command
 */
namespace ChainOfresposibility2
{
    public class Class1
    {
    }
}
